name: Privacy Policy Drift Check

on:
  schedule:
    - cron: '0 6 * * *'  # Daily at 6am UTC
  workflow_dispatch:
    inputs:
      target_repo:
        description: 'Target repository URL to check'
        required: true
        type: string
      policy_path:
        description: 'Path to privacy policy (relative to target root)'
        required: true
        default: 'PRIVACY.md'
        type: string

permissions:
  contents: read
  issues: write

jobs:
  drift-check:
    runs-on: ubuntu-latest
    # Skip scheduled runs when no target is configured via repository variables
    if: ${{ inputs.target_repo != '' || vars.DRIFT_CHECK_TARGET_REPO != '' }}
    steps:
      - name: Checkout auditor repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -e ".[auditor]"

      - name: Determine target
        run: |
          TARGET_REPO="${{ inputs.target_repo || vars.DRIFT_CHECK_TARGET_REPO }}"
          POLICY_PATH="${{ inputs.policy_path || vars.DRIFT_CHECK_POLICY_PATH || 'PRIVACY.md' }}"

          if [ -z "$TARGET_REPO" ]; then
            echo "::error::No target repository configured. Set DRIFT_CHECK_TARGET_REPO repository variable or use workflow_dispatch."
            exit 1
          fi

          if [[ "$TARGET_REPO" == http* ]]; then
            git clone "$TARGET_REPO" /tmp/audit-target
            TARGET_PATH="/tmp/audit-target"
          else
            TARGET_PATH="$TARGET_REPO"
          fi

          echo "TARGET_PATH=$TARGET_PATH" >> $GITHUB_ENV
          echo "POLICY_PATH=$POLICY_PATH" >> $GITHUB_ENV

      - name: Check for drift
        id: drift
        run: |
          RESULT=$(mvs audit drift-check \
            --target "$TARGET_PATH" \
            --policy "$TARGET_PATH/$POLICY_PATH" \
            --json)
          echo "drift_result=$RESULT" >> $GITHUB_OUTPUT
          echo "$RESULT" | python3 -c "import sys,json; r=json.load(sys.stdin); sys.exit(1 if r['has_drift'] else 0)" || echo "DRIFT_DETECTED=true" >> $GITHUB_ENV

      - name: Create re-audit issue
        if: env.DRIFT_DETECTED == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          EXISTING=$(gh issue list --label "privacy-drift" --state open --json number --jq '.[0].number // empty')
          if [ -z "$EXISTING" ]; then
            gh issue create \
              --title "Privacy audit drift detected — re-audit required" \
              --label "privacy-drift,privacy-audit" \
              --body "$(cat <<BODY
          ## Privacy Audit Drift Detected

          The privacy policy or code has changed since the last audit.

          $(echo '${{ steps.drift.outputs.drift_result }}' | python3 -c "import sys,json; r=json.load(sys.stdin); print(r['details'])")

          ### Action Required
          Run a new privacy audit:
          \`\`\`
          gh workflow run privacy-audit.yml -f target_repo=... -f policy_path=$POLICY_PATH
          \`\`\`

          ---
          *Generated by privacy drift check*
          BODY
          )"
          else
            gh issue comment "$EXISTING" --body "Drift still detected as of $(date -u +%Y-%m-%d). Re-audit pending."
          fi
